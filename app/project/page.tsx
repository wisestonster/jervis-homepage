import Image from "next/image"; import { ContactCTA, JsonLd, PageHero } from "@/components/page-ui"; import { projectCases, projects } from "@/lib/content"; import { breadcrumbJsonLd, createPageMetadata } from "@/lib/seo";
export const metadata=createPageMetadata({title:"프로젝트",description:"메인넷, 미디어 프로토콜, NFT와 토큰화 서비스 등 저비스랩스가 수행한 블록체인 프로젝트와 사례를 소개합니다.",path:"/project"});
export default function ProjectPage(){return <><JsonLd data={breadcrumbJsonLd([{name:"홈",path:"/"},{name:"프로젝트",path:"/project"}])}/><PageHero dark eyebrow="03. PROJECT" title={<>검증된 기술,<br/>실제 비즈니스 성과</>} description="메인넷, 미디어 프로토콜, NFT와 토큰화 서비스까지 다양한 산업의 프로젝트를 수행해왔습니다."/><section className="section projects-section"><div className="container project-list">{projects.map((project,index)=><article key={project.name}><span>0{index+1}</span><div><small>{project.type}</small><h2>{project.name}</h2><p>{project.summary}</p></div><ul>{project.details.map(item=><li key={item}>{item}</li>)}</ul></article>)}</div></section><section className="section section--subtle"><div className="container"><div className="section-heading"><p className="eyebrow">USE CASES</p><h2>산업을 확장한 프로젝트 사례</h2></div><div className="case-grid">{projectCases.map((c)=><article key={c.title}><Image src={c.image} width={72} height={72} alt={c.alt}/><h3>{c.title}</h3><p>{c.copy}</p></article>)}</div></div></section><ContactCTA/></>}
